let left_operand = 10;
let right_operand = 25;
let operator = "/"
let ans;

//let operator = ("what is the value of the result? ");  

if (operator == "+") {
     ans = left_operand + right_operand;

}else if (operator == "-") {
     ans = left_operand - right_operand;

}if (operator == "*") {
     ans = left_operand * right_operand;

}else if (operator == "/") {
     ans = left_operand / right_operand;
}

  console.log(ans)	


